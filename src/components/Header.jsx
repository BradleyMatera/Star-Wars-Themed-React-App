import React from "react";
import { HeaderWrapper, Logo } from "../styles/HeaderStyledComponents";

const Header = () => {
  return (
    <HeaderWrapper>
      <Logo>Star Wars Themed React App</Logo>
      {/* Add other header elements here */}
    </HeaderWrapper>
  );
};

export default Header;
